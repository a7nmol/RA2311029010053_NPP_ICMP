# RA2311029010053_NPP_ICMP
This Java GUI project simulates how packets travel through a network using TTL and demonstrates ICMP error handling. It shows packet movement across multiple routers step by step. It also visualizes whether the packet reaches the destination or gets dropped due to TTL expiration.
